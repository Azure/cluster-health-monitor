---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checknodehealth-controller
  namespace: kube-system
  labels:
    app: checknodehealth-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: checknodehealth-controller
  template:
    metadata:
      labels:
        app: checknodehealth-controller
    spec:
      serviceAccountName: checknodehealth-controller
      containers:
        - name: controller
          image: carlostest3acr.azurecr.io/cluster-health-monitor:latest
          command: ["/controller"]
          args:
            - "-v=5"
            - "-enable-node-reboot-check"
            - "-enable-node-condition"
            - "-enable-gpu-health-check"
          imagePullPolicy: Always
          env:
            - name: CHECKER_POD_IMAGE
              value: "carlostest3acr.azurecr.io/cluster-health-monitor:latest"
          resources:
            requests:
              cpu: 20m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
